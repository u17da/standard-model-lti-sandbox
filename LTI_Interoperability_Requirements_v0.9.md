# 学習eポータルとツールのLTI連携要件一覧

本要件一覧は、「初等中等教育におけるシステム間連携のための相互運用標準モデル Ver. 5.00」に基づき、LTI 1.3接続に係るプラットフォーム（学習eポータル）と一般学習ツールの連携要件を抽出したものです。

## 1. 共通技術要件

### 1.1 LTI仕様
- **LTI v1.3 Core**: 接続のベースとして準拠すること (MUST)

#### 1.1.1 LTI v1.3 Core 規格の主要な要求フロー
- **サードパーティ開始ログイン（OIDC Third-Party Initiated Login）**: プラットフォームから開始されるログインプロトコルをサポートすること。
- **署名済み IDトークン（JWT）による認可**: メッセージや情報を保護するため、署名されたJWTを用いること。
- **公開鍵配信（JWKS）**: 公開鍵のセット（JWK Set）を用いて署名を検証する仕組みをサポートすること。

### 1.2 ユーザー識別要件
- **sub (Subject) クレーム**: 
  - システム間でのユーザー特定のため、各ユーザーに割り当てられた **UUID (Version 4)** を用いること (REQUIRED)
  - 識別子の優先順位：
    1. 連携する校務支援システムにて生成された UUID
    2. プラットフォーム（学習eポータル）にて生成された UUID
- **UUIDの品質**: RFC 4122に準拠し、暗号論的疑似乱数生成器 (CSPRNG) を用いて生成すること。

### 1.3 ロール要件
- **roles クレーム**: 1EdTechが定義するフルURL形式のロールを用いること (REQUIRED)
- **主要なロールの解釈**:
  - 児童生徒: `.../person#Student` または `.../membership#Learner`
  - 教職員: `.../person#Faculty` または `.../membership#Instructor`

### 1.4 デプロイメントID要件
- **deployment_id クレーム**: 組織のコードを含めること (MUST)
- **命名規則**: `[接頭辞]_[コード]`
  - `S_`: 学校コード (文科省学校コード)
  - `B_`: 自治体コード (総務省全国地方公共団体コード)
  - `P_`: 都道府県コード (総務省全国地方公共団体コード)

---

## 2. プラットフォーム (学習eポータル) への要件

### 2.1 学習ツールとの連携
- **LTI Resource Link Launch Request**: LTI v1.3 Coreに基づきツールを呼び出せること (MUST)

#### 2.1.1 プラットフォーム側の実装必須項目
- **Login Initiation Request**: ツールに対して `iss`, `login_hint`, `target_link_uri` を含むリクエストを送信すること。
- **JSON Web Token (JWT) の生成**: 
  - ヘッダーに適切な `kid`（鍵ID）を含めること。
  - RS256等のアルゴリズムで署名すること。
- **IDトークン クレームの正確性**: 標準モデルで規定された `sub` (UUID) や `deployment_id` (組織コード) を正しく含めること。
- **JWKSエンドポイント**: 自身の公開鍵を `application/json` 形式で配信すること。

### 2.2 属性情報の共有と匿名利用
- **属性情報の提供**: ツールから要求があった場合、以下の情報を送信できること (REQUIRED/OPTIONAL)
  - `name`: 表示名（通称・ニックネーム可）
  - `grade`: 学年コード（資料3：APPLIC/文科省コード準拠）
  - `classname`: 主所属クラス名
- **匿名利用**: `sub`を含まない「Anonymous launch」を許容するツールへの対応。

---

## 3. ツール (学習ツール) への要件

### 3.1 呼び出しへの応答
- **LTI Resource Link Launch Request**: プラットフォームからの呼び出しに対して適切にコンテンツを提供すること (MUST)

#### 3.1.1 ツール側の実装必須項目
- **OIDC Initiation 受付**: プラットフォームからの開始リクエストを受け取り、指定された `target_link_uri` にリダイレクトすること。
- **IDトークンの検証**:
  - **署名検証**: プラットフォームの公開鍵（JWKS）を取得し、JWTの署名が正しいか検証すること。
  - **iss / aud 検証**: 発行元および自身のクライアントIDが正しいか確認すること。
  - **有効期限（exp / iat）検証**: トークンが期限内であることを確認すること。
  - **Nonce検証**: セッションに保持した `nonce` とトークン内の値が一致することを確認すること。
- **ユーザー識別**: 受信した `sub` (UUID) のみを識別子として用いること。

### 3.2 情報の取り扱い
- プラットフォームから送信された属性情報（`name`, `grade`等）を使用する場合、個人情報の取り扱いについて事前に合意が必要となる。使用しない場合は破棄すること。
- プラットフォームから `sub` クレームが送られない場合は、匿名ユーザー（Anonymous launch）として処理すること。

---

## 4. 現在のシステムの検証状況

本テストシステムにおける、上記要件への対応状況は以下の通りです。

| 要件カテゴリ | 項目 | ステータス | 備考 |
| :--- | :--- | :---: | :--- |
| **1. 共通要件** | 1.1 LTI v1.3 Core 準拠 | △ | プラットフォームは実装済み。ツール側での厳密な署名・Nonce検証が必要。 |
| | 1.2 sub (UUID v4) | ○ | `src/users.js` にて UUID v4 を定義し、正しく送出。 |
| | 1.3 roles (フルURL) | ○ | 1EdTech規定のURL形式で送出。 |
| | 1.4 deployment_id (S_記法) | ○ | 学校コードにプレフィックス `S_` を付与して生成。 |
| **2. プラットフォーム** | 2.1.1 実装必須項目 (JWKS等) | ○ | OIDC, JWT署名, JWKS配信を実装済み。 |
| | 2.2 氏名・学年等の送信 | △ | 氏名・学年を送信中。詳細なキー名の整合性は継続確認。 |
| | 2.2 匿名利用のサポート | × | 現状、テストユーザー選択を必須としている。 |
| **3. ツール** | 3.1.1 実装必須項目 (署名検証等) | △ | JWTのパースは可能だが、公開鍵による自動検証は未実装。 |
| | 3.2 氏名・学年情報の非永続化 | ○ | 受信情報の表示のみに使用。 |
| | 3.2 匿名利用の処理 | × | `sub` 欠如時のフォールバック処理が未実装。 |

※ ○: 対応済み、△: 一部対応・簡易実装、×: 未対応

---

## 5. 未充足要件への対応方針

現状のシステムで「×」または「△」となっている項目を解消するための、技術的な実装指針です。

### 5.1 ツール側での厳密なJWT検証 (△)
現在の簡易デコードから、LTI 1.3仕様に完全準拠した検証処理へアップグレードする必要があります。
- **JWKSクライアントの実装**: `jwks-rsa` 等を利用し、プラットフォームの `/api/platform/jwks` から動的に公開鍵を取得して署名を検証する。
- **Nonce/State検証**: 初期リクエスト時に生成した `nonce` と `state` をセッション（またはCookie/DB）に一時保存し、Launch受信時に一致することを確認する実装を追加する。
- **属性チェック**: `iss` (発行元URL) および `aud` (自身のClient ID) が期待通りであることを検証するロジックを `api/tool.js` に組み込む。

### 5.2 匿名利用 (Anonymous launch) への対応 (×)
標準モデルでは、ユーザーを特定しない形でのツール利用（サンプル版の試用など）を想定しています。
- **プラットフォーム側**: 
  - ユーザー選択画面に「匿名で起動（テストユーザーなし）」のオプションを追加する。
  - `sub` クレームを含まない（または匿名用IDを含む）IDトークンを生成し、ツールに送信する。
- **ツール側**: 
  - `sub` が存在しない Launchリクエストを受信した場合、エラー画面ではなく「ゲストモード」としてコンテンツを表示するフォールバック処理を `handleToolLaunch` に実装する。

### 5.3 属性情報の整合性確保 (△)
- **クレーム名の確定**: 現在 `applic_grades` としている学年情報を、まなびポケット等の実稼働環境に近い形式（例：特定のカスタム名前空間内）へ整理する。
- **学年・クラス情報の活用**: ツール側で受信した学年コード（P1〜H3）に応じたコンテンツの出し分け（フィルタリング等）の実装を行い、データ連携の有用性を検証可能にする。
