# 学習eポータルとツールのLTI連携要件一覧 (Ver. 1.1)

本要件一覧は、LTI標準モデル v5.00 に準拠し、最新のテストシステム実装（証明書検証、オートログイン、厳格な検証ロジック等）を反映した最新の要件定義（1.1版）です。

## 1. 共通技術要件

### 1.1 LTI仕様
- **LTI v1.3 Core**: 接続のベースとして準拠すること (MUST)
- **Security**: システム全体のセキュリティと完全性を確保するための最新のパッチと検証アルゴリズムを適用すること。

#### 1.1.1 LTI v1.3 Core 規格の主要な要求フロー
- **サードパーティ開始ログイン（OIDC Third-Party Initiated Login）**: プラットフォームから開始されるログインプロトコルをサポートすること。
- **署名済み IDトークン（JWT）による認可**: メッセージや情報を保護するため、署名されたJWTを用いること。
- **公開鍵配信（JWKS）**: 公開鍵のセット（JWK Set）を用いて署名を検証する仕組みをサポートすること。

### 1.2 ユーザー識別要件
- **sub (Subject) クレーム**: 
  - システム間でのユーザー特定のため、各ユーザーに割り当てられた **UUID (Version 4)** を用いること (REQUIRED)
  - 識別子の優先順位：
    1. 連携する校務支援システムにて生成された UUID
    2. プラットフォーム（学習eポータル）にて生成された UUID
- **UUIDの品質**: RFC 4122に準拠し、暗号論的疑似乱数生成器 (CSPRNG) を用いて生成すること。

### 1.3 ロール要件
- **roles クレーム**: 1EdTechが定義するフルURL形式のロールを用いること (REQUIRED)
- **主要なロールの解釈**:
  - 児童生徒: `.../person#Student` または `.../membership#Learner`
  - 教職員: `.../person#Faculty` または `.../membership#Instructor`

### 1.4 デプロイメントID要件
- **deployment_id クレーム**: 組織のコードを含めること (MUST)
- **命名規則**: `[接頭辞]_[コード]`
  - `S_`: 学校コード (文科省学校コード)
  - `B_`: 自治体コード (総務省全国地方公共団体コード)
  - `P_`: 都道府県コード (総務省全国地方公共団体コード)

---

## 2. プラットフォーム (学習eポータル) への要件

### 2.1 学習ツールとの連携
- **LTI Resource Link Launch Request**: LTI v1.3 Coreに基づきツールを呼び出せること (MUST)
- **柔軟なリダイレクト**: 開発環境（`localhost:3000`等）へのリダイレクトを許容する柔軟な検証ルールを持つこと。

#### 2.1.1 プラットフォーム側の実装必須項目
- **Login Initiation Request**: ツールに対して `iss`, `login_hint`, `target_link_uri` を含むリクエストを送信すること。
- **JSON Web Token (JWT) の生成**: 
  - ヘッダーに適切な `kid`（鍵ID）を含めること。
  - RS256アルゴリズムで署名すること。
- **IDトークン クレームの正確性**: 標準モデルで規定された `sub` (UUID) や `deployment_id` (組織コード) を正しく含めること。
- **JWKSエンドポイント**: 自身の公開鍵を `application/json` 形式で配信すること。

### 2.2 属性情報の共有と匿名利用
- **属性情報の提供**: ツールから要求があった場合、以下の情報を送信できること (REQUIRED/OPTIONAL)
  - `name`: 表示名（通称・ニックネーム可）
  - `grade`: 学年コード（資料3：APPLIC/文科省コード準拠）
  - `classname`: 主所属クラス名
- **匿名利用**: `sub`に特異的なUUIDを用い、個人情報を除外した「匿名起動」をサポートすること。

---

## 3. ツール (学習ツール) への要件

### 3.1 呼び出しへの応答
- **LTI Resource Link Launch Request**: プラットフォームからの呼び出しに対して適切にコンテンツを提供すること (MUST)

#### 3.1.1 ツール側の実装必須項目
- **OIDC Initiation 受付**: プラットフォームからの開始リクエストを受け取り、適切に応答すること。
- **IDトークンの厳格な検証**:
  - **署名検証**: プラットフォームの公開鍵（JWKS）を取得し、JWTの署名が正しいか検証すること。
  - **iss / aud 検証**: 発行元および自身のクライアントIDが正しいか確認すること。
  - **有効期限（exp / iat）検証**: トークンが期限内であることを確認すること。
  - **Nonce/State検証**: セッションに保持した値と一致することを確認すること。
- **シームレスなログイン**: LTI Launch情報に基づき、Firebase等の認証基盤と連携して自動ログイン（オートログイン）を実現すること。

---

## 4. 現在のシステムの検証状況

本テストシステムにおける、上記要件への対応状況は以下の通りです。

| 要件カテゴリ | 項目 | ステータス | 備考 |
| :--- | :--- | :---: | :--- |
| **1. 共通要件** | 1.1 LTI v1.3 Core 準拠 | ○ | JWKS検証、Nonce/State、および主要パラメータ（scope/prompt等）の厳格な検証を実装済み。 |
| | 1.2 sub (UUID v4) | ○ | `src/users.js` にて UUID v4 を定義し、正しく送出。 |
| | 1.3 roles (フルURL) | ○ | 1EdTech規定のURL形式で送出。 |
| | 1.4 deployment_id (S_記法) | ○ | 学校コードにプレフィックス `S_` を付与して生成。 |
| **2. プラットフォーム** | 2.1.1 実装必須項目 (JWKS等) | ○ | OIDC, JWT署名, JWKS配信、および不正な client_id/redirect_uri の拒絶を実装済み。 |
| | 2.1.1 リダイレクトURI緩和 | ○ | `localhost:3000` などのローカル開発環境への遷移をサポート。 |
| | 2.2 氏名・学年等の送信 | ○ | 姓・名の分離、名前空間付きカスタムクレームによる送信を実装。 |
| | 2.2 匿名起動 (Anonymous) | ○ | `sub` を匿名用IDに切り替え、個人情報を除外するオプションを実装済み。 |
| **3. ツール** | 3.1.1 実装必須項目 (署名等) | ○ | `jwks-rsa` を用いた動的な鍵取得と厳格なJWT検証を実装。 |
| | 3.1.1 オートログイン | ○ | LTI Launch成功後、Firebaseカスタムトークンを発行し自動ログインを実現。 |
| | 3.1.1 証明書検証 (Zero Burden) | ○ | QRコードによるサーバーサイド接続確認に対応。ツール側の改修不要で検証可能。 |
| | 3.2 匿名利用の処理 | ○ | 匿名Subject受信時の「ゲストモード」表示およびオンボーディングスキップを実装。 |

※ ○: 対応済み、△: 一部対応・簡易実装、×: 未対応

---

## 5. 達成済みおよび今後の拡張方針

これまでの開発により、主要なLTI相互運用性要件はすべて「○（対応済み）」に到達しました。

### 5.1 達成済みの重要機能
- **厳密なJWT検証**: `api/tool.js` において、`jwks-rsa` を利用した公開鍵の動的取得と、LTI 1.3仕様に完全準拠した検証ロジックを実装しました。
- **匿名起動とプライバシー**: ユーザーを特定せずにツールを試用できる「匿名起動」機能をプラットフォーム・ツール双方で実装しました。
- **証明書検証システム**: ツールプロバイダーがコード変更なしでプラットフォームとの接続を確認できる仕組みを構築しました。
- **オートログイン連携**: LTIのアイデンティティを既存の認証基盤（Firebase）へシームレスに引き継ぐ仕組みを確立しました。

### 5.2 今後の拡張（Optional）
- **Deep Linking**: コンテンツ選択ワークフローの追加。
- **Assignment and Grades Service (AGS)**: 成績返却機能の実装。
- **Names and Role Provisioning Services (NRPS)**: メンバーシップ情報のリスト取得対応。
