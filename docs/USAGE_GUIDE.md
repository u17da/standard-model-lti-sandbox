# （非公式）相互運用標準モデル LTI接続テストツール 利用ガイド

本ドキュメントでは、学習eポータル「相互運用性標準モデル (V5.00)」に準拠したLTI接続テストツールの利用方法について解説します。

---

## 📚 概要
このツールは、デジタル教科書や教材ツールが、日本の学習eポータル標準モデルに準拠したLTI 1.3接続を行えるかを検証するためのサンドボックス（シミュレータ）環境です。

**主な機能:**
- **LTI Platform シミュレーション**: ツールに対してOIDCログイン開始、LTI Launchリクエストを送信します。
- **意図的なエラー生成**: `prompt=none` の欠落や不正な `scope` を送信し、ツール側のバリデーション動作を確認できます。
- **診断ツール**: JWKS（公開鍵）エンドポイントの到達性確認が可能です。
- **接続証明書発行**: 接続成功時に、設定内容を記録した証明書と検証用QRコードを発行します。

---

## 🚀 クイックスタート

本システムはクラウド上にデプロイされており、セットアップ不要で利用可能です。

### 1. アクセス
ブラウザで以下のURLにアクセスします。
- **オンライン環境 (推奨)**: [https://standard-model-lti-sandbox.ecube.jp/](https://standard-model-lti-sandbox.ecube.jp/)
- **ローカル環境 (開発者用)**: `http://localhost:4000` (リポジトリをクローンして実行した場合)

> [!NOTE]
> 通常の接続検証には、最新の安定版が動作している**オンライン環境**の利用を推奨します。ローカル環境は、ソースコードの変更を伴う開発やデバッグの際に利用してください。

### 2. サンプルツールでの動作確認（デモ）
まずは内蔵されている「サンプルツール」を使って、正常なLTI接続の流れを確認することをお勧めします。

1. **デモ設定の自動入力**
   - 画面左上の「🧪 サンプルツールでの動作デモ」エリアにある **[デモ用設定値を自動入力]** ボタンをクリックします。
   - 「接続設定」フォームに、サンプル用のURL等が自動的に入力されます。

2. **ツールの起動**
   - 画面下部の **[ツールを起動]** ボタンをクリックします。
   - 別ウィンドウ（またはタブ）が開き、LTI接続シーケンスが開始されます。

3. **シーケンスの確認**
   - メイン画面右側の「Sequence Visualizer」で、`Initiation` → `Auth Request` → `LTI Launch` とステップが進む様子を確認できます。
   - ログエリアには、やり取りされたパラメータの詳細が表示されます。

---

## ⚙️ 詳細設定マニュアル

ご自身が開発されているツールをテストする場合の設定項目です。

### 1. 接続設定
| 項目名 | 説明 |
| :--- | :--- |
| **ツール名称** | 接続証明書に記載されるツールの名前です（任意）。 |
| **Initiation URL** | LTI 接続を開始するためのエンドポイント（OIDC Login URL）。ツールが最初に受け取るリクエスト先です。 |
| **Target Link URI** | 認証成功後にリダイレクトされる、ツールのメイン画面（Launch URL）です。 |
| **Client ID** | `standard-test-client` （固定）。ツール側の設定で使用してください。 |

### 2. ユーザー選択
テストに使用するユーザー（教員・児童生徒）を切り替えられます。
それぞれのユーザーには、標準モデルで定義された役割（Roles）や学年情報（Custom Claims）が紐付いています。

- **教員**: 佐藤 太郎 他
- **児童・生徒**: 小学6年生、中学2年生、高校3年生など（学年による挙動の違いをテスト可能）

### 3. 意図的なエラー (デバッグ用)
ツールが不正なリクエストを正しく拒否できるかテストするためのオプションです。
通常はチェックを外しておきますが、堅牢性テストの際に使用します。

- **prompt=none を含めない**: 仕様必須の `prompt=none` を除外してリクエストします。
- **scope を不正にする**: `openid` 以外の値を送信します。

---

## 🛠️ 診断ツール: JWKS Check
LTI 1.3 では、OIDC認証のためにツールの公開鍵（JWKS）にプラットフォームからアクセスできる必要があります。
「診断ツール」エリアにJWKS URLを入力し **[確認]** を押すことで、プラットフォームから正しくJSONが取得できるかチェックできます。

---

## ❓ トラブルシューティング

接続がうまくいかない場合、ログエリアまたはツール側に表示されるエラーを確認してください。
本ツールは、よくあるミスに対して「ヒント」を表示する機能を備えています。

### よくあるエラーと対処法

| エラー内容 | 原因と対策 |
| :--- | :--- |
| **MISSING_PROMPT** / **WRONG_PROMPT** | **原因**: 認証リクエストに `prompt=none` が含まれていません。<br>**対策**: 学習eポータル標準ではサイレントログインが必須です。OIDCリクエストパラメータを確認してください。 |
| **MISSING_SCOPE** / **WRONG_SCOPE** | **原因**: `scope` が `openid` になっていません。<br>**対策**: LTI 1.3 では `openid` スコープが必須です。 |
| **MISSING_NONCE** / **MISSING_STATE** | **原因**: セキュリティパラメータが不足しています。<br>**対策**: リプレイ攻撃防止の `nonce` と、CSRF対策の `state` を必ず生成・送信してください。 |
| **WRONG_RESPONSE_MODE** | **原因**: `response_mode=form_post` 以外が指定されています。<br>**対策**: セキュリティ上の理由から、GETではなくPOST (`form_post`) でのレスポンスが推奨されます。 |
| **404 Not Found** (Authorize) | **原因**: 認証エンドポイントがPOSTを受け付けていない可能性があります。<br>**対策**: 本ツールの `/api/platform/oauth/authorize` はGET/POST両対応ですが、Initiation時の指定を確認してください。 |
| **JWConnection Error** | **原因**: ツール側のJWKS URLにアクセスできません。<br>**対策**: ファイアウォール設定や、HTTPS証明書（自己署名など）の問題がないか確認してください。「診断ツール」で確認可能です。 |

---

## 📱 モバイル検証について
接続テスト完了後に表示される「接続証明書」には検証用QRコードが含まれています。
スマートフォンでQRコードを読み取ることで、モバイル端末上での証明書表示確認も簡単に行えます。
